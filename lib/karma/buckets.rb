module Karma
  
  # The buckets that are defined on the karma server. In the future, these
  # will probably be retrieved from the server rather than hard-coded.
  BUCKETS = [:comments, :edits]

end
  