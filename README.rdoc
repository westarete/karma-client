= karma-client

The interface that web applications use to interact with karma-server, which
provides a means of evaluating and maintaining the reputation of users across
several cooperating web applications.
